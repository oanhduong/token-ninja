# Sample of real-world commands that should match a local rule. One per line.
# Comments (lines starting with #) are ignored by the coverage test.

# ── shell basics ──────────────────────────────────────────────────────
ls
ls -la
ls -lh
ls src
pwd
whoami
id
hostname
uname -a
uptime
date
env
which node
mkdir -p dist
touch .gitkeep
cp package.json pkg.backup
mv foo.txt bar.txt
stat README.md
file LICENSE
echo hello
history

# ── filesystem ────────────────────────────────────────────────────────
cat README.md
head -n 20 file.txt
tail -n 50 log.txt
tail -f app.log
wc -l src/*.ts
sort -u names.txt
uniq -c
diff a.txt b.txt
diff -u a.txt b.txt
md5sum file.bin
sha256sum file.bin
du -sh .
df -h
find . -name "*.ts"
find . -type f -name "*.md"
tree -L 2

# ── text ──────────────────────────────────────────────────────────────
grep -r TODO src
grep -rn "export function" src
rg -n "TODO" .
rg --files
awk '{print $1}' file.txt
tr -d '\r' < file.txt
xxd -p binary.dat
jq '.' package.json
jq -r '.name' package.json

# ── process ───────────────────────────────────────────────────────────
ps aux
ps -ef
pgrep -f node
pidof node
kill -l

# ── network ───────────────────────────────────────────────────────────
ping -c 3 google.com
dig example.com
nslookup example.com
host example.com
whois example.com
ip a
netstat -tnlp
ss -tulpn
curl -I https://example.com
curl -s https://api.example.com/status

# ── git read-only ─────────────────────────────────────────────────────
git status
git status -s
git st
git diff
git diff --staged
git diff --name-only
git log
git log --oneline
git log --oneline --graph
git log --oneline -n 10
git show HEAD
git branch
git branch -a
git branch -vv
git branch --show-current
git remote -v
git remote show origin
git config --list
git stash list
git ls-files
git blame src/router/index.ts
git reflog
git shortlog -sn
git describe --tags
git tag -l
git rev-parse HEAD
git rev-parse --abbrev-ref HEAD
git merge-base main HEAD
git whatchanged -n 5
git worktree list
git submodule status
git fsck
git count-objects -vH

# ── git write ─────────────────────────────────────────────────────────
git add .
git add -A
git add -p
git add src/router/index.ts
git commit -m "fix: a thing"
git commit -am "chore: bump"
git commit --amend --no-edit
git checkout main
git checkout -b feature/x
git switch main
git switch -c feature/y
git restore --staged README.md
git merge feature/x
git merge --ff-only main
git pull
git pull --rebase
git push
git push origin main
git push -u origin feature/x
git push --force-with-lease
git push --tags
git fetch
git fetch --all --prune
git clone https://github.com/user/repo
git tag v1.0.0
git stash
git stash pop
git stash apply
git rebase main
git rebase --continue
git cherry-pick abc1234
git revert abc1234
git mv old.txt new.txt
git rm file.txt
git init

# ── gh ────────────────────────────────────────────────────────────────
gh auth status
gh repo view
gh pr list
gh pr view 42
gh pr checkout 42
gh issue list
gh issue view 100
gh run list
gh workflow list
gh release list

# ── npm / yarn / pnpm / bun ───────────────────────────────────────────
npm install
npm i
npm ci
npm install -D vitest
npm uninstall lodash
npm test
npm start
npm run build
npm run dev
npm run lint
npm list
npm ls
npm outdated
npm audit
npm view express
npm init -y
npm --version
npm whoami
npx vitest
yarn install
yarn add lodash
yarn add -D vitest
yarn remove lodash
yarn test
yarn build
yarn dev
yarn list
yarn outdated
yarn audit
yarn why lodash
yarn workspaces list
pnpm install
pnpm i
pnpm add zod
pnpm add -D vitest
pnpm remove lodash
pnpm test
pnpm build
pnpm dev
pnpm list
pnpm outdated
pnpm audit
pnpm store prune
pnpm why lodash
bun install
bun add zod
bun remove lodash
bun test
bun build
bun pm ls

# ── python ────────────────────────────────────────────────────────────
python -V
python3 --version
python -m http.server 8080
python -c "print(1)"
pip install requests
pip install -r requirements.txt
pip install -e .
pip uninstall requests
pip list
pip freeze
pip check
pytest
pytest -v
pytest -k test_foo
pytest --cov
python -m pytest
python -m unittest
poetry install
poetry add requests
poetry remove requests
poetry update
poetry show
poetry run pytest
uv sync
uv add requests
uv run pytest
uv pip list
conda list
conda env list
ruff check
ruff format .
black .
black --check
mypy src
pylint src
isort src
flake8 src

# ── java / kotlin ─────────────────────────────────────────────────────
java -version
javac -version
mvn compile
mvn test
mvn package
mvn clean install
mvn dependency:tree
gradle build
gradle test
gradle clean
gradle dependencies
./gradlew build
./gradlew test
kotlinc -version

# ── rust / go / ruby / php ────────────────────────────────────────────
cargo build
cargo build --release
cargo run
cargo test
cargo check
cargo clippy
cargo fmt
cargo fmt --check
cargo doc
cargo tree
cargo update
rustc --version
rustup show
go build
go run .
go test ./...
go test -v
go mod tidy
go mod download
go vet ./...
go fmt ./...
go version
go env
golangci-lint run
ruby -v
gem list
bundle install
bundle exec rspec
rake -T
rails routes
rails db:migrate
rspec
php -v
composer install
composer update
composer show
php artisan migrate
php artisan route:list

# ── docker / k8s ──────────────────────────────────────────────────────
docker ps
docker ps -a
docker images
docker logs container
docker logs -f container
docker inspect container
docker stats --no-stream
docker build -t myimage .
docker pull alpine
docker tag a b
docker rm container
docker rmi image
docker run -it alpine
docker stop container
docker restart container
docker system df
docker info
docker volume ls
docker network ls
docker compose up -d
docker compose down
docker compose ps
docker compose logs
kubectl get pods
kubectl get svc
kubectl get deployments
kubectl get nodes
kubectl get ns
kubectl describe pod mypod
kubectl logs mypod
kubectl logs -f mypod
kubectl apply -f deploy.yaml
kubectl port-forward pod/mypod 8080:80
kubectl config get-contexts
kubectl config current-context
kubectl top pods
kubectl top nodes
kubectl version
kubectl cluster-info
helm list
helm version
helm repo list

# ── database read ─────────────────────────────────────────────────────
psql --version
mysql --version
redis-cli ping
redis-cli info
redis-cli get key1
mongosh --version
sqlite3 app.db ".tables"
sqlite3 app.db ".schema"

# ── build / test / lint ───────────────────────────────────────────────
make
make test
make clean
cmake .
cmake --build build
ninja
bazel build //...
bazel test //...
sbt compile
sbt test
lein deps
jest
vitest
vitest run
mocha tests
cypress run
playwright test
eslint .
eslint --fix .
prettier --check .
prettier --write .
rubocop
shellcheck script.sh
hadolint Dockerfile
yamllint config.yaml
markdownlint README.md

# ── editors / system / archive ────────────────────────────────────────
code .
code src/cli.ts
lscpu
lsmem
lsblk
free -h
vmstat
systemctl --user status
journalctl --user -n 50
tar -tf archive.tar.gz
tar -xzf archive.tar.gz
tar -czf out.tar.gz dir
zip -r out.zip dir
unzip archive.zip
unzip -l archive.zip
gzip file.txt
gunzip file.txt.gz
7z l archive.7z
7z x archive.7z

# ── natural language ──────────────────────────────────────────────────
run the tests
run tests
test it
build the project
build it
compile it
install dependencies
install deps
list dependencies
show dependencies
list files
show me the files
what's in this directory
what changed
show diff
show the diff
what branch am I on
current branch
show recent commits
who wrote this file src/cli.ts
search for TODO
find files named index.ts
what's running
disk usage
start the server
run dev
show running containers
list images
show pods
list services

# ── cloud CLIs ────────────────────────────────────────────────────────
aws sts get-caller-identity
aws s3 ls
aws ec2 describe-instances
aws iam list-users
aws lambda list-functions
aws logs describe-log-groups
aws eks list-clusters
aws rds describe-db-instances
aws cloudformation list-stacks
aws --version
az account show
az group list
az vm list
az aks list
az --version
gcloud auth list
gcloud config list
gcloud projects list
gcloud compute instances list
gcloud container clusters list
gcloud version
gsutil ls gs://my-bucket
vercel ls
vercel logs my-project
vercel whoami
netlify status
netlify sites:list
heroku apps
heroku logs --tail
heroku ps
fly status
fly apps list
flyctl status
railway status
supabase status
doctl compute droplet list

# ── IaC ───────────────────────────────────────────────────────────────
terraform plan
terraform init
terraform fmt -check
terraform validate
terraform workspace list
terraform output -json
terraform state list
terraform providers
terraform version
tofu plan
tofu validate
ansible --version
ansible-playbook --syntax-check site.yml
ansible-inventory --list
ansible-galaxy list
ansible-lint
vagrant status
vagrant global-status
vagrant box list
pulumi preview
pulumi stack ls
pulumi whoami
packer validate template.json
cdk synth
cdk diff
cdk ls

# ── bundlers ──────────────────────────────────────────────────────────
vite build
vite preview
vite --version
turbo build
turbo run test
turbo --version
esbuild --version
parcel build src/index.html
parcel --version
rollup -c
webpack --mode production
webpack serve
rspack build
rsbuild build
tsup src/index.ts
nx list
nx run myapp:build
nx affected --target=test

# ── Deno ──────────────────────────────────────────────────────────────
deno --version
deno info
deno test
deno fmt --check
deno lint
deno check
deno task build
deno run --allow-net script.ts

# ── Elixir / Erlang ───────────────────────────────────────────────────
mix --version
mix deps.get
mix test
mix compile
mix format --check-formatted
mix credo
mix phx.server
mix phx.routes
mix ecto.migrate
elixir --version
rebar3 compile
rebar3 eunit

# ── Dart / Flutter ────────────────────────────────────────────────────
dart --version
dart pub get
dart analyze
dart format .
dart test
flutter --version
flutter doctor
flutter doctor -v
flutter devices
flutter pub get
flutter clean
flutter analyze
flutter test
flutter build apk
flutter run

# ── process supervisors ───────────────────────────────────────────────
pm2 list
pm2 status
pm2 logs
pm2 start ecosystem.config.js
pm2 restart all
systemctl --user list-units
systemctl --user status myapp
systemctl status nginx
systemctl list-timers
journalctl --user -u myapp
journalctl -u nginx
launchctl list
overmind ps
overmind start
foreman start
supervisorctl status

# ── env management ────────────────────────────────────────────────────
direnv status
direnv allow
asdf current
asdf list
asdf plugin list
asdf install nodejs 20.11.0
mise ls
mise current
mise install node@20
mise use python@3.12
mise env
nvm current
nvm ls
pyenv versions
pyenv which python
rbenv versions
jenv versions
nix --version
nix flake show
nix flake check
nix-env -q
nix-channel --list
sdk current
sdk list
conda info --envs
conda list
conda env list
mamba list

# ── distributed systems ───────────────────────────────────────────────
consul version
consul members
consul catalog services
consul kv get foo
etcdctl version
etcdctl endpoint status
etcdctl endpoint health
etcdctl member list
etcdctl get /config/foo
zkServer.sh status
nats --version
nats server check
nats stream ls
kafka-topics.sh --list
kcat -L
rabbitmqctl status
rabbitmqctl list_queues

# ── database extras ───────────────────────────────────────────────────
pg_isready
pg_isready -h localhost -p 5432
pg_config --version
atlas --version
atlas clusters list
atlas auth whoami
cockroach version
cockroach node status
clickhouse-client --version
influx version
influx bucket list
cqlsh --version
nodetool status
nodetool info
aws dynamodb list-tables
aws dynamodb describe-table --table-name users
duckdb --version
prisma --version
prisma generate
prisma migrate status
npx prisma generate
drizzle-kit check
drizzle-kit generate
sequelize-cli db:migrate:status
knex migrate:list

# ── python extras ─────────────────────────────────────────────────────
pdm --version
pdm install
pdm lock
pdm add requests
pdm list --tree
hatch --version
hatch env show
hatch run test
pipenv --version
pipenv graph
pipenv install
pipx list
pipx install black
pipx install ruff
pyright --version
pyright src
pylint src
bandit -r src

# ── build-tools extras ────────────────────────────────────────────────
just --list
just --summary
just test
task --list
task -l
task build
meson --version
meson compile
meson test
gradle --version
gradle tasks
./gradlew build
./gradlew test
mvn --version
mvn clean package
mvn test
mvn dependency:tree
./mvnw test

# ── modern linters ────────────────────────────────────────────────────
biome --version
biome check .
biome check --write .
biome lint .
biome format --write .
biome ci .
oxlint src
dprint check
dprint fmt
dprint --version
actionlint .github/workflows/ci.yml
jsonlint package.json
gitleaks detect
gitleaks version
pre-commit --version
pre-commit run --all-files
pre-commit install

# ── modern-cli (ripgrep, fd, bat, eza, zoxide, delta, fzf, jq, yq, …) ─
rg --version
rg pattern
rg -i TODO src
rg --type-list
fd --version
fd ".*\.ts"
fd . src
bat --version
bat README.md
bat --list-themes
eza --version
eza -la
exa --long
zoxide --version
delta --version
fzf --version
jq --version
jq '.name' package.json
yq --version
hyperfine --version
tldr --version
tldr git
dust --version
duf
ncdu --version
btop --version
neofetch --version
glow --version
hexyl --version

# ── make / just / task extras ──────────────────────────────────────────
make -p
make -q
make --help
make list
make targets
make fmt
make lint
make release
make install
make vendor
make coverage
just --summary
just --variables
just --choose
task --help
task --summary
task --status
mage --version
mage -h
buck2 --version

# ── helm + kustomize + helmfile ───────────────────────────────────────
helm version
helm version --short
helm env
helm list
helm list -A
helm ls
helm history myrelease
helm get all myrelease
helm get values myrelease
helm repo list
helm repo update
helm search repo nginx
helm plugin list
helm dependency list
helm dep list
helm lint .
helm template .
kustomize version
kustomize build .
helmfile --version
helmfile list
helmfile lint

# ── gitops (argocd, flux, skaffold, tilt, fastlane, pre-commit) ───────
argocd version
argocd context
argocd app list
argocd proj list
argocd repo list
argocd app get myapp
argocd app diff myapp
flux --version
flux version
flux check
flux get all
flux get all -A
flux get sources all
flux get kustomizations
flux stats
skaffold version
skaffold diagnose
skaffold config list
skaffold schema list
tilt version
tilt doctor
tilt get all
fastlane --version
fastlane lanes
fastlane actions
fastlane env
pre-commit sample-config
pre-commit validate-config
pre-commit autoupdate
pre-commit clean

# ── secrets-infra (vault, nomad, packer, sops, op, bw) ────────────────
vault version
vault status
vault token lookup
vault auth list
vault secrets list
vault policy list
vault kv list secret
vault kv get secret/foo
vault read sys/health
nomad version
nomad status
nomad node status
nomad server members
nomad agent-info
nomad job status myjob
nomad deployment status
nomad eval list
nomad namespace list
nomad job inspect myjob
nomad alloc status abc
packer -v
packer plugins installed
sops --version
age --version
op --version
op account list
op vault list
bw --version
bw status

# ── container-tools (podman, nerdctl, buildah, skopeo, crane, dive) ──
podman --version
podman version
podman info
podman ps
podman ps -a
podman images
podman image ls
podman volume ls
podman network ls
podman pod ps
podman machine list
nerdctl version
nerdctl ps
nerdctl images
buildah version
buildah containers
buildah images
skopeo --version
skopeo -v
crane version
crane --version
dive --version
trivy --version
cosign version
grype --version
syft --version
docker compose version
docker compose ps
docker compose ls
docker compose config
docker compose top
docker compose images

# ── k8s-tools (kubectl extras, kubectx, k9s, kind, minikube, k3d) ────
kubectl version --short
kubectl version --client
kubectl config current-context
kubectl config get-contexts
kubectl config view --minify
kubectl config get-clusters
kubectl config get-users
kubectl cluster-info
kubectl api-resources
kubectl api-versions
kubectl auth whoami
kubectl auth can-i --list
kubectl get pods
kubectl get pods -A
kubectl get pods -o wide
kubectl get svc
kubectl get deploy
kubectl get ns
kubectl get nodes
kubectl get nodes -o wide
kubectl get events
kubectl get cm
kubectl get secrets
kubectl get pv
kubectl get pvc
kubectl get ingress
kubectl get sa
kubectl get crd
kubectl top nodes
kubectl top pods
kubectx
kubectx -c
kubens
kubens -c
kubecolor --version
k9s version
lens --version
k0s version
k0s status
kind --version
kind get clusters
minikube version
minikube status
minikube profile list
minikube addons list
minikube ip
k3d --version
k3d cluster list
colima version
colima status
stern --version
velero version
velero backup get

# ── git-extras (version, config, log/diff/show/branch/tag, plumbing) ──
git --version
git version
git help log
git help rebase
git help merge
git help stash
git config --global --list
git config user.email
git config user.name
git config core.editor
git config remote.origin.url
git log --oneline -10
git log --oneline --graph
git log --oneline --graph --decorate --all
git log --stat
git log --patch
git log --name-only
git log --since=1.week
git log HEAD..origin/main
git log origin/main..HEAD
git diff --stat
git diff --numstat
git diff --name-only
git diff --cached
git diff --staged
git diff HEAD
git diff HEAD~1
git diff main
git diff origin/main
git diff origin/main...HEAD
git show HEAD
git show HEAD~1
git show --stat HEAD
git show --name-only HEAD
git remote -v
git remote show origin
git remote get-url origin
git ls-remote
git ls-remote --heads
git branch
git branch -a
git branch -r
git branch -v
git branch --merged
git branch --no-merged
git branch --show-current
git branch --sort=-committerdate
git tag
git tag -l
git tag --sort=-creatordate
git status
git status -s
git status -sb
git status --short
git status --porcelain
git status --porcelain=v2
git rev-parse HEAD
git rev-parse --short HEAD
git rev-parse --abbrev-ref HEAD
git rev-parse --show-toplevel
git rev-parse --is-inside-work-tree
git rev-list --count HEAD
git for-each-ref
git for-each-ref refs/heads
git show-branch
git symbolic-ref HEAD
git symbolic-ref --short HEAD
git cat-file -t HEAD
git cat-file -p HEAD
git grep TODO
git blame src/cli.ts
git gc
git gc --auto
git prune --dry-run
git maintenance run
git maintenance status

# ── editor extras (version/help queries) ──────────────────────────────
code --version
code -v
code --list-extensions
cursor --version
nvim --version
nano --version
emacs --version
subl --version
idea --version
pycharm --version
goland --version
helix --version
hx --version
micro --version
zed --version

# ── rust / cargo extras ───────────────────────────────────────────────
cargo --list
cargo metadata
cargo metadata --no-deps
cargo search serde
cargo pkgid
cargo verify-project
cargo locate-project
cargo publish --dry-run
cargo package --list
cargo outdated
cargo deny check
cargo nextest list
rustc --print sysroot
rustc --print target-list
rustup show active-toolchain
rustup target list
rustup target list --installed
rustup check
sccache --show-stats

# ── deno / dart / flutter / kotlin / elixir extras ────────────────────
deno info --json
deno help
deno completions bash
deno check --all
deno test --watch
dart help
dart info
dart pub deps --json
dart pub publish --dry-run
dart fix --apply --dry-run
dart compile js
dart devtools --version
flutter doctor --verbose
flutter pub upgrade --major-versions
flutter upgrade --dry-run
flutter gen-l10n
fvm --version
fvm list
fvm doctor
kotlin --version
kotlinc --version
gradle tasks --all
gradle projects
gradle properties
gradle dependencyInsight
./gradlew ktlintCheck
./gradlew detekt
mix help deps
mix deps.get --only prod
mix test --failed
mix test --stale
mix credo suggest
mix ecto.create
mix ecto.drop
mix hex.info
mix hex.outdated
mix local.hex --version
rebar3 tree
rebar3 xref

# ── test-runner extras ────────────────────────────────────────────────
vitest list
vitest bench
vitest typecheck
mocha --version
cypress info
cypress verify
playwright show-report
ava --version
tap --version
tap list
rspec --dry-run
pytest --collect-only
pytest --markers
pytest --fixtures
tox -l
nox -l
go test ./... -race

# ── gh extras (codespace, gist, search, label, project) ───────────────
gh --version
gh version
gh auth token
gh config list
gh status
gh extension list
gh alias list
gh ssh-key list
gh gpg-key list
gh pr list --draft
gh pr status
gh gist list
gh codespace list
gh cs list
gh browse
gh search repos topic:ai
gh label list
gh project list

# ── shell-advanced extras (tmux, screen, zellij, history, shopt) ──────
tmux -V
tmux --version
tmux ls
tmux list-sessions
tmux list-panes
tmux list-windows
tmux list-commands
tmux list-keys
tmux show-options -g
tmux show-environment
tmux info
screen -v
screen -ls
zellij --version
zellij list-sessions
zellij ls
wezterm --version
history
history 20
alias
type ls
type git
command -v node
shopt -p
ulimit -a
ulimit -n
umask
umask -S
printenv
printenv PATH
locale
locale -a
jobs
jobs -l
